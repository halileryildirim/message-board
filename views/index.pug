extends layout

block content
  h1= title
  p Welcome to #{title}
  each message in messages
    h3 #{message.user} 
    p #{message.text}
    p #{message.added}
  button 
    a(href="/new") Add Message